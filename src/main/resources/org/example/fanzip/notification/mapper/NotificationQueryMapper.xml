<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.fanzip.notification.mapper.NotificationQueryMapper">

    <select id="findInfluencerNameById" parameterType="long" resultType="string">
        SELECT influencer_name
        FROM influencers
        WHERE influencer_id = #{influencerId}
        LIMIT 1
    </select>
    <select id="findActiveSubscriberTokens" resultType="java.lang.String">

        SELECT DISTINCT upt.push_token
        FROM memberships m
                 JOIN user_push_token upt ON upt.user_id = m.user_id
        WHERE m.influencer_id = #{influencerId}
          AND m.status = 'ACTIVE'
          AND upt.push_token IS NOT NULL
    </select>

</mapper>
